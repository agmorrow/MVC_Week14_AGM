<div class="welcomeBox">
<h1 id="welcomeId">Meet TheTechBlog</h1>
<p id="welcomeMsg"> TheTechBlog is a blog site you can use to publish a blog post or comment on other developers blog posts.
</p>
</div>

<div class="container post">
  <div class="row justify-content-evenly">
    {{#each posts as |post| }}
      <div class="card postCard">
        <div class="card-header">
          {{post.title}}
        </div>
        <div class="card-body">
          <h5 class="card-title">Created By: {{post.user.username}}</h5>
          <p class="card-text">{{post.content}}</p>
          <a href="#" class="btn btn-primary viewPostBtn " id="viewPostBtn" data-id={{post.id}}>View Post</a>
        </div>
        <div class="card-footer">
          Date Created: {{format_date post.dateCreated}}
        </div>
      </div>
    {{/each}}
  </div>
</div>